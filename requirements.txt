# ============================================================
# Frequency-Spatial Vision Mamba Dual Watermarking
# Requirements — Pin versions for reproducibility
# ============================================================

# Core Deep Learning
torch>=2.0.0
torchvision>=0.15.0

# Segmentation (U-Net with ResNet-34 backbone for ROI)
segmentation-models-pytorch>=0.3.3

# Wavelet Transform
PyWavelets>=1.4.1

# Image processing & metrics
scikit-image>=0.21.0
Pillow>=10.0.0
scipy>=1.11.0
opencv-python>=4.8.0

# Cryptography (AES-256-GCM + HKDF)
cryptography>=41.0.0

# Tensor manipulation (einops for Mamba rearrangements)
einops>=0.7.0

# Training utilities
tqdm>=4.66.0
tensorboard>=2.14.0
matplotlib>=3.7.0
numpy>=1.24.0

# Notebook support
ipykernel>=6.25.0
ipywidgets>=8.1.0

# Typing / code quality
typing_extensions>=4.7.0
